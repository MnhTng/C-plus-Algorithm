#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    while( t-- ){
        long long x, n;
        cin >> x >> n;

        cout << pow( x, n ) << endl;
    }
    system("pause");
    return 0;
}